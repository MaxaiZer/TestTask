port: 8080
mode: debug
jwt:
  access_lifetime: 900  # 15 minutes
  refresh_lifetime: 604800  # 7 days
  secret_key: secret_key_for_development
  issuer: test-task.com
  audience: myapp
db:
  connection_string: "host=localhost port=5432 dbname=test_db user=postgres password=postgres"